import 'package:flutter/material.dart';

class EpisodeInfoScreen extends StatelessWidget {
  const EpisodeInfoScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
